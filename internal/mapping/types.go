package mapping

type (
    // folder title as a key, component title as a value
    ComponentsMapping = map[string]string

    // file extension as as a key, category(language or file type) name as a value
    ExtensionsMapping = map[string]string
)
